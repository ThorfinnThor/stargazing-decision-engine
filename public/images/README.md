# Self-hosted image assets

Approved destination and site images are stored here as WebP files after manual
source review. The current image manifest is intentionally pending; no remote
image is fetched at page-render time.
