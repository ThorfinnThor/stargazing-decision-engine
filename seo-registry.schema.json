{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://stargazing.local/schema/seo-registry.json",
  "title": "SEO Registry",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "siteUrl", "pages"],
  "properties": {
    "version": { "const": 1 },
    "siteUrl": { "type": "string", "pattern": "^https://" },
    "pages": { "type": "array", "items": { "$ref": "#/$defs/page" } }
  },
  "$defs": {
    "page": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "pageType", "locale", "path", "canonical", "alternatePaths", "title", "h1", "description", "indexable", "reasons", "structuredDataType"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "pageType": { "type": "string", "minLength": 1 },
        "locale": { "enum": ["en", "de"] },
        "path": { "type": "string", "pattern": "^/" },
        "canonical": { "type": "string", "pattern": "^https://" },
        "alternatePaths": { "type": "object", "additionalProperties": { "type": "string", "pattern": "^/" } },
        "title": { "type": "string", "minLength": 1 },
        "h1": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 1 },
        "indexable": { "type": "boolean" },
        "reasons": { "type": "array", "items": { "type": "string" } },
        "structuredDataType": { "const": "WebPage" }
      }
    }
  }
}
